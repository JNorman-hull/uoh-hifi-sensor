### use the sensor selection as the main interface which persists through whole session

## selection options used to filter the index, index represented  by tiles of index > deployment_id, shows all at this level 
# > dt table using our selection module, enable an action on a double click of a row > goes to sensor dashboard