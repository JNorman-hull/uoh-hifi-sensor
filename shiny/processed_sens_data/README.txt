Global and instrument index mirror those stored on OneDrive
CSV data not stored locally
Move CSV data here if needed when using local distribution

1. Add blank global and instrument index files (remove _BLANK from filename)
2. Add example raw data
3. Ensure path in server.R is local e.g., raw_data_path <- reactive("./raw_sens_data")
4. Run App
